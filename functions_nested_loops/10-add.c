#include "main.h"

/**
 * add - adds two integers.
 * @a: the first integer
 * @b: the second integer
 *
 * Description: adds two integers.
 * Return: the result
 */
int add(int a, int b)
{
	return (a + b);
}
